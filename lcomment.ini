project.name = lcomment

project.default_charset = utf-8

server.index = 192.168.31.92:8383

server.search = 192.168.31.92:8384

[comment_id]
type = id
tokenizer = none

[origin_id]
index = self
tokenizer = full

[commented_id]
index = self
tokenizer = full

[parent_id]
index = self
tokenizer = full

[user_id]
index = self
tokenizer = full

[commented_user]
index = self
tokenizer = full

[content]
index = mixed

[status]
index = self
tokenizer = full

[createtime]
type = numeric
index = self
tokenizer = full